import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function CiFailuresPage() {
  return (
    <PagePlaceholder
      title="CI Failures"
      description="Ingested CI pipeline failures awaiting analysis and triage."
    />
  );
}
