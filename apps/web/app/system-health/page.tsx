import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function SystemHealthPage() {
  return (
    <PagePlaceholder
      title="System Health"
      description="Live status of API, workers, scheduler, and dependencies."
    />
  );
}
