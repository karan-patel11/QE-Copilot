import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function OverviewPage() {
  return (
    <PagePlaceholder
      title="Overview"
      description="High-level status across CI failures, test generation, and triage."
    />
  );
}
