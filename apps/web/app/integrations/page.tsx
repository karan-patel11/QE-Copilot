import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function IntegrationsPage() {
  return (
    <PagePlaceholder
      title="Integrations"
      description="Connect source control, CI providers, and issue trackers."
    />
  );
}
